# Include top-level documentation
include README.md
include LICENSE
include ERRARA.md

# Include example scripts (but not install them as modules)
include listener.py
include tx.py

# Exclude development/editor files
exclude .gitignore
exclude .bashrc
exclude sx1262-driver.code-workspace

# Exclude any archive or legacy directories if they exist
prune archive
prune legacy
